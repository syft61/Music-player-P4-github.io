/* Static Text
- Texting Aspect Ratio
- introduce single and multipule executed code
- activity: do another rectangle with tet in it
- copy and paste dynamic program
*/
// global variables
int appWidth, appHeight;
float titleX, titleY, titleWidth, titleHeight;
float superCoolPhraseX, superCoolPhrase, superCoolPhraseWidth, superCoolWidthPhraseHeight;
float footerX, footerY, footerWidth, footerHeight;
//
//Display
size( 500, 700); //Portrait CANVAS
appWidth = width;
appHeight = height;
//
//population
titleX = appWidth*1/10;
titleY = appHeight*1/10;
titleWidth = appWidth*8/10;
titleHeight = appHeight*1/10;
superCoolPhraseX = titleX;
superCoolPhrase = appHeight*4.5/10;
superCoolPhraseWidth = titleWidth;
superCoolWidthPhraseHeight = titleHeight;
footerX = titleX;
footerY = appHeight*8/10;
footerWidth = titleWidth;
footerHeight = titleHeight;
//DIVs
rect( titleX, titleY, titleWidth, titleHeight );
rect( superCoolPhraseX, superCoolPhrase, superCoolPhraseWidth, superCoolWidthPhraseHeight );
rect( footerX, footerY, footerWidth, footerHeight );
